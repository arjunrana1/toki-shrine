# Current work

- **Active task:** [TS-P7-stats-feedback-final](tasks/TS-P7-stats-feedback-final/TASK.md) — Phase 7 Stats, feedback and final pass.
- **State:** `awaiting_owner` — review of the Phase 7 working-tree submission (base `12ee572`) is **PASS WITH NOTES** (P7-N1 engagement events re-log on activity recreation, matching the accepted `settings_viewed` semantics; P7-N2 Feedback from Settings pops to Settings, not home). All focus areas verified: Stats §9 figures/formulas/exclusions against the persistence-events contract, §13 mailto intent with handoff-only `feedback_sent`, retirement of `bubble_dragged`/`challenge_abandoned` with no remaining emitters and behavior/historical rows preserved, collapsed variant seam with production values re-pinned in tests, migration posture (fallback removed, v3 schema baseline checked in, entities unchanged so in-field v3 opens preserved), accessibility touches. Reviewer re-ran non-device checks: 194/194 JVM (UP-TO-DATE, XML-counted), `git diff --check` clean, hex/manifest/dependency audits clean. See [REVIEW](tasks/TS-P7-stats-feedback-final/REVIEW.md); submission details in [HANDBACK](tasks/TS-P7-stats-feedback-final/HANDBACK.md). Reviewer identity GLM 5.3 — **model-diverse (Codex) confirmation pending** on the migration-posture, event-retirement and transactional-Stats areas before closure.
- **Next actor:** Arjun — optionally route the REVIEW to Codex for the pending model-diverse confirmation, then own device acceptance via [OWNER-CHECKS](tasks/TS-P7-stats-feedback-final/OWNER-CHECKS.md) starting with P7-O1 (`adb install -r` data preservation), plus any build installation. No repair is requested; nothing is committed yet.
- **Phase 5 closure:** [TS-P5-owner-corrections](tasks/TS-P5-owner-corrections/TASK.md) is `closed`: repair `68fdcb3` passed independent review (PASS WITH NOTES, RV2-N1–N3, recorded as future-phase obligations) and on 23 September 2026 Arjun reported Phase 5 owner testing complete and passing on the installed review-attributed build, closing P5C-O14. Owner findings P5C-O1–O14 are all resolved and accepted; typing paste suppression and autocorrect safeguards remain as accepted.
- **Phase 6 closure:** [TS-P6-pause-lifecycle](tasks/TS-P6-pause-lifecycle/TASK.md) is `closed`; P6C-N1 is an accepted/deferred theoretical edge, and P6C-O4/P6-O13 remain explicit do-not-fix cases.
- **Implementation boundary:** Phase 7 owns Stats, Feedback, production-value restoration, the approved event retirement/audit, accessibility/final UI work and release migration/distribution obligations. PostHog/remote analytics, accounts, rename and unrelated features remain out of scope.
- **Phase 4 retained risk:** P4-09 Samsung overnight survival remains owner-accepted deferred risk, not a pass. Do not represent it as validated; see the closed [Phase 4 checklist](tasks/TS-P4-detection-engine/OWNER-CHECKS.md). The Phase 6 foreground pause service inherits this same environmental risk for countdown expiry.
- **Device state:** at Phase 3 closure, 51 Android tests passed on SM-S918B (`R5CW30ZBM2R`, Android 16) and packages were removed. Arjun later installed a build at records-only `aa6e91c` (code equivalent to `9b3da43`). On 20 September 2026, Codex ran the authorized targeted `AssetDetectionConfigLoaderTest` suite on that same device: **5/5 passed**. Gradle then removed both app and test packages (`pm path` empty); `enabled_accessibility_services` is `null`. On 21 September 2026, at Arjun's request, the Phase 5B debug APK built at `c5a8e9e` (records-only over reviewed repair `7a8fb00`) was installed on that device via `adb install -r` (Success). Accessibility re-grant and any seeding remain owner setup; the app was not launched and no device testing was performed. On 23 September 2026, a fresh debug APK for the P5C corrections was first staged while the phone was absent, then installed the same day after it was connected: `adb -s R5CW30ZBM2R install -r` returned **Success** (streamed install, data preserved, no uninstall/clear). APK `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `1077fb1b32d660d7bd05038c10648371dbebeba460b43d97670ef1445040376b` verified unchanged before install, built from the `7fbb692` tree (Gradle up-to-date against records-only HEAD `5eb27ed`, so it contains exactly the reviewed code), `com.arjunrana.tokishrine` versionName 0.1.0 / versionCode 1 (debug). `pm path` confirms the app package; the test package is absent. The app was not launched; accessibility re-grant and any seeding remain owner setup, and no device testing was performed. A direct owner correction then moved the gate headline cluster to the top segment (commit `9d411d4`, layout-only, assembleDebug verified) and a second made app-triggered pause completions land on the triggering app (commit `2543329`, assembleDebug + 146/146 JVM verified; site triggers keep the old finish pending an owner decision). The newest APK (SHA-256 `9542463a8a645a8360d1bab1c214869006503461427973c3e28a2d1405314d66`) was installed the same day via `adb install -r` (Success); that build served the P5C retests (O1–O13). After the P5C-O14 review PASS, the current installed build is a fresh debug APK for the reviewed repair, built from the tree at records-only HEAD `497fb5a` (app source identical to reviewed `68fdcb3`; `git diff 68fdcb3..HEAD -- app/` empty): `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `34717bd89a2fd85bdd65e688d42ba438eeb8d7b42f2cc8ca78561865b47290a5`, installed 23 September 2026 via `adb -s R5CW30ZBM2R install -r` (Success, streamed install, data preserved, no uninstall/clear). `pm path` confirms `com.arjunrana.tokishrine` versionName 0.1.0 / versionCode 1 (debug); `enabled_accessibility_services` retains `com.arjunrana.tokishrine/.TokiAccessibilityService` across the reinstall, though actual service re-binding is owner observation. That installed build served the passing P5C-O14 owner retest and the Phase 5 closure report. On 23 September 2026, at Arjun's request, the Phase 6 acceptance build was installed from the reviewed `062c71e` app tree: a fresh debug APK built from records-only HEAD `5f31511` (`git diff 062c71e..HEAD -- app/` empty and `./build.sh assembleDebug` up-to-date against that tree, so it contains exactly the reviewed code), `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `f637c82e883137e99031ce11712347a0154c254f34aa92b49fd11c263e47346c`, `com.arjunrana.tokishrine` versionName 0.1.0 / versionCode 1 (debug). Installed via `adb -s R5CW30ZBM2R install -r` (Success, streamed install, data preserved, no uninstall/clear). `pm path` confirms the package; no test package is present on the primary user; `enabled_accessibility_services` retains `com.arjunrana.tokishrine/.TokiAccessibilityService` across the reinstall, though actual service re-binding is owner observation. The app was not launched and no device testing was performed; accessibility re-binding and any seeding remain owner setup. This installed build served the full Phase 6 checklist run (results in the task's [OWNER-CHECKS](tasks/TS-P6-pause-lifecycle/OWNER-CHECKS.md)). On 23 September 2026 corrected APKs for the owner corrections were built and staged — first `ac78985` (SHA-256 `9083e321…5f29d`, never installed), then superseded by `7abb516` with the owner-approved bubble drag-to-dismiss (`app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `df50470ebc4432458b9ce8c9eaed64978de82cc75c6ed31a74579f24ee8af590`) while the phone was absent. The `7abb516` APK was then installed the same day after reconnection: `adb -s R5CW30ZBM2R install -r` returned **Success** (streamed install, data preserved, no uninstall/clear; hash re-verified unchanged before install). `pm path` confirms `com.arjunrana.tokishrine`; `enabled_accessibility_services` retains `com.arjunrana.tokishrine/.TokiAccessibilityService` across the reinstall, though actual service re-binding is owner observation. The app was not launched and no device testing was performed. Two further same-day reinstalls followed on the owner's request, each hash re-verified before install, each `adb -s R5CW30ZBM2R install -r` → **Success** (streamed install, data preserved, no uninstall/clear): `b1d9ec8` (SHA-256 `ac5d34fc…a341e`, Dismiss label + 0.2 zone transparency) and finally `7fb322e` (SHA-256 `67d09fa6e7d611d6409a028c224eb323fd84a329fad1be4ba7298b97b17448cf`, turn-off challenge visual corrections P6C-O6). `pm path` confirms `com.arjunrana.tokishrine`; `enabled_accessibility_services` retains `com.arjunrana.tokishrine/.TokiAccessibilityService` across the reinstalls, though actual service re-binding is owner observation. Nothing was launched and no device testing was performed; any owner testing alongside the then-pending Codex verification carries that `7fb322e` build attribution. On 23 September 2026, after the `7fb322e..f7bb1f2` review **PASS WITH NOTES**, Arjun requested the repair build's installation: a fresh debug APK built from records-only HEAD `138360f` (`git diff f7bb1f2..HEAD -- app/` empty and `./build.sh assembleDebug` up-to-date against that tree, so it contains exactly the reviewed repair code, including P6C-O7), `app/build/outputs/apk/debug/app-debug.apk`, SHA-256 `f39dca91be335b0ecfd38cdebdb329110fa3a4543e97540004b28afee5e97e08`, hash re-verified unchanged immediately before install. Installed via `adb -s R5CW30ZBM2R install -r` → **Success** (streamed install, data preserved, no uninstall/clear). `pm path` confirms `com.arjunrana.tokishrine`; `enabled_accessibility_services` retains `com.arjunrana.tokishrine/.TokiAccessibilityService` across the reinstall, though actual service re-binding is owner observation. The app was not launched and no device testing was performed. The device now runs the `f7bb1f2` code; the P6C-R1/P6C-O7 owner retest (ordinary dismissal, subsequent-pause bubble return, `bubble_dismissed` attribution, Dismiss-label readability) is recorded with this build attribution. See [build/validation](../docs/components/build-validation.md).

## Closed Phase 3 and wizard evidence

- Arjun reports all P3-01–P3-15 and BW-01–BW-10 acceptance checks passing, including the revised ON haptic, overlay deny/grant/revoke flow and DC-06 sheet darkness.
- [Phase 3 REVIEW](tasks/TS-P3-validation/REVIEW.md) records final PASS. `./build.sh testDebugUnitTest` passed 60/60 at that time. Executed Android evidence passed 51/51: BlockRepositoryTest 23, EventRepositoryTest 9, CreateFlowScreenTest 19.
- P3-NV-02: enabled-state/event and onboarding marker/event commit, rollback and at-most-once behavior executed against Room, including forced event failures.
- P3-NV-03: owner callback/resume/recreation paths plus executed restored-pending Room failure/retry evidence establish one durable permission outcome.
- P3-NV-04: the complete revised ownership/save/UI/recreation suite executed successfully.
- [Wizard REVIEW](tasks/TS-block-wizard-redesign/REVIEW.md) records WZ-F01/WZ-F02 and DC-06 PASS plus instrumented closure. No prior-phase blocker remains.

Workflow migration completed 18 September 2026 is documentation-only; see [migration record](../docs/history/migration-2026-09-18/MIGRATION.md). Current-state changes belong here, not in AGENTS or global handoffs.
