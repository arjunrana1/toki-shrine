# Historical decision record

Historical evidence, not current workflow instructions. Later records may supersede this entry. Use current component contracts and PRD requirements for implementation.

Source: pre-migration DECISIONS.md, section 13.

## Phase 2 repair finalized — 12 September 2026

- **Repair (this commit, on top of a0df32c).** Both 10 September blockers closed in `CreateFlowScreen.kt`: (1) a synchronous one-shot terminal guard in `CreateFlowState` — `abandonAndClose()` captures the exit step and takes the guard before launching the async event write, `save()` shares the guard, so repeated Back/Save input while logging is suspended yields exactly one terminal event and one navigation; a rejected save releases the guard; (2) target selection is disabled until the ownership maps load (nullable maps in the app picker and site field), and a save rejected with `ConflictingOwnershipException` keeps the editor open, retains the draft, and shows the exact inline string "Already added to a block" on the review step — no owner name, dialog or transfer. Back from step 4 clears the notice.
- **Test seams (`open`).** `BlockRepository` (`appOwnerships`/`siteOwnerships`), `EventRepository` (`log`) and `InstalledAppsRepository` (`loadApps`/`labelFor`) are `open` solely so instrumented UI tests can gate async reads/writes; production uses the classes directly. No behavior change.
- **Owner dependency approval (12 September 2026).** `androidx.compose.ui:ui-test-junit4` (androidTestImplementation) and `androidx.compose.ui:ui-test-manifest` (debugImplementation) — BOM-versioned Compose artifacts for the create-flow UI regression harness.
- **Test evidence (completed builder run, 10 September 2026 — attributed, not rerun).** 7 JVM + 25 instrumented green on the USB S23 Ultra / Android 16: 12 repository (including the new site-conflict rollback-after-earlier-app-mutations test comparing every stored field and ownership before/after), 6 event, 7 UI (new CreateFlowScreenTest: repeated Back/Save exactly-once under suspended writes, delayed ownership loading for apps and sites, rejected-save draft retention + retry, search/step Back and edit-cancellation event sequences, retired conflict-event suppression). Supersedes the 17-instrumented count that described the a0df32c-era suite.
- **Codex re-review of the uncommitted repair: PASS WITH NOTES** (owner-reported). Owner device validation pending; Phase 3 not cleared.
- **Fixture state at finalization.** The 10 September harness run uninstalled the app and wiped its data. The pre-run backup at `/tmp/toki-fixture-backup/toki-shrine.db` (The scroll pit, News spiral) was verified intact on 12 September but has since been purged by macOS /tmp cleanup — restoration is no longer possible. The approved build was installed fresh on 12 September and launches with an empty database; the owner recreates checklist fixtures.
