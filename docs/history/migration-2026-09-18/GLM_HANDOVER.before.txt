# GLM builder handoff

**Phase 3 code PASS — 14 September 2026, `cb1ad45` on `4b0ca0f`.** Codex re-reviewed only `4b0ca0f..cb1ad45`: no blocking findings. Serialized remove-after-success settlement closes the permission outcome-loss/duplication window, with failure, cancellation and partial-retry JVM coverage; delayed `launchResolved` assignment closes the first-launch recreation race and handles restored non-root stacks. Builder result XML records **44 JVM tests, zero failures/errors**; assembleDebug, assembleDebugAndroidTest compilation and hex grep remain builder-reported. Codex ran no builds/tests/device operations. Arjun may begin Phase 3 device/experience validation; code PASS is not full phase sign-off. Do not perform further builder work unless a concrete device failure is returned.

**Phase 3 repair re-review — 14 September 2026: CHANGES REQUIRED at `4b0ca0f` (`9a88b41..4b0ca0f`).** The saveable route stack, single-flight terminal actions and atomic block/onboarding transitions close the prior navigation/duplication blocker. One bounded lifecycle/event correction remains: `PermissionEventLogic.resolve/settle` removes/clears pending permission identities before `emitOutcome` succeeds, so cancellation or an event insert failure makes the required outcome unretryable; emit first and remove that permission only after success, preserving unprocessed entries, with targeted failure/retry tests. In `MainActivity`, move `launchResolved = true` after the suspended onboarding-completion read and route decision (and resolve restored non-root stacks without querying) so recreation during the read retries rather than skipping first-run Welcome. Preserve all other repair code and Phase 2 behavior. Re-review the new diff against `4b0ca0f`; no device work, and owner device testing remains blocked until code clearance.

**Phase 3 repair submitted — 14 September 2026, `4b0ca0f` on `9a88b41`.** Both original blockers were addressed in one bounded pass, no Phase 4/branding/dependency changes: (1) recreation — saveable string-coded route stack (`ui/navigation/Routes.kt`: Route + ChecklistMode + RouteCodec + RouteStackSaver), Welcome as a real route with a saved `launchResolved` flag, saveable pending-permission list injected into `PermissionEventLogic`, saveable battery-picker selection, and `requestPermission` ordering of mark → durable `permission_requested` → launch with rollback on failure; (2) terminal actions — `ui/util/TerminalAction` single-flight runner in the activity lifecycle scope, atomic `BlockRepository.setEnabledRecordingTransition`, and atomic at-most-once `EventRepository.completeOnboarding`. Checks reported: assembleDebug, 39 JVM tests green (4 RouteCodec + 5 TerminalAction + 13 PermissionEventLogic + 17 existing), assembleDebugAndroidTest compile-only, hex grep clean. No device operations. Details and caveats are in DECISIONS.md "Phase 3 repair".

**Phase 3 review — 14 September 2026: CHANGES REQUIRED at `9a88b41` on base `6c3b962`.** Codex reviewed `6c3b962..9a88b41` code-only. Two blockers: (1) `MainActivity` route/onboarding state and `PermissionEventLogic.pendingRequests` use plain composition memory, so activity/process recreation can return to the wrong screen and lose the required permission outcome event; preserve the minimal route/mode/request state across recreation and pin restoration/settlement with targeted tests. (2) Turn-on/off and onboarding completion are not single-flight or navigation-safe: repeated taps can duplicate state events/haptics, and Back/Not yet can dispose the composable coroutine between the block write and event; two successful turn-on jobs can call `onClose` twice. Make each terminal transition exactly once, prevent cancellation/extra navigation while pending, and cover rapid repeat/cancel/failure paths. Preserve the nominal gate, four-row UI, Phase 2 behavior and unrelated working-tree docs/branding. Use GLM 5.3 for this bounded lifecycle/state repair; stop after scoped checks and a repair commit for diff re-review. No device work.

**Original Phase 3 submission — `9a88b41`.** GLM 5.3 built onboarding and permissions only: screens 1–4 and 24, accessibility-gated activation through every entry path (both ON toggles route to the checklist when accessibility is missing; the turn-on confirmation re-checks before persisting), activation haptics moved after successful persistence (on and off), permission state read from the system and refreshed on every app resume, §10 onboarding/permission events via JVM-tested `PermissionEventLogic`, and the minimal accessibility-service declaration required to make the permission grantable at all (no detection logic — Phase 4). Reported checks: assembleDebug, 24 JVM tests green (7 Stats + 5 Domain + 7 PermissionEventLogic + 5 OemBattery), androidTest compile, hex grep clean. Existing result XML records 24 JVM tests with zero failures/errors; Codex did not execute them. No device operations; instrumented execution and all permission behaviour pending owner validation. Code committed; DECISIONS.md "Phase 3 implementation" section records the details among uncommitted documentation changes.

**Prior checkpoint — 13 September 2026: code PASS, `6c3b962`.** Codex reviewed `084b5eb..6c3b962`: unchanged-edit fixture corrected, legacy typo normalization explicitly covered, enabled-state assertions corrected, helper copy updated, and interior multiline website paste rejected without merging. No blocking findings in the corrective diff. Phase 3 implementation is cleared from `6c3b962`; verify git state. GLM reports debug build and 12 JVM tests passing, instrumented sources compiled only. Codex ran no builds/tests/device operations. Latest UI regressions and paste behavior remain pending owner device confirmation; this is implementation clearance, not a new device pass. Haptic tuning remains pending; Phase 3 must emit activation success feedback after permission checks and successful persistence.

**Corrective pass submitted — 13 September 2026, `72de89c` on `084b5eb` (awaiting Codex re-review of `084b5eb..72de89c`).** Only `CreateFlowScreen.kt` and `CreateFlowScreenTest.kt` changed: the unchanged-edit fixture seeds `showTypos = true` (draft helper gained a parameter) so `fields_changed = none` matches production; new `editRewritesLegacyShowTyposFalseToTrue` keeps explicit legacy-false→true coverage; the two website-test Add gates and the friction-entry Next check wait on `assertIsEnabled()` instead of a click action; the Disable-this-block helper reads "this block" in both sentences (PRD addendum corrected to match). Checks: assembleDebug, testDebugUnitTest (12 JVM, executed green), assembleDebugAndroidTest — compilation only, execution pending. No device operations.

**Owner clarification follow-up — 13 September 2026, `6c3b962` (include in the same re-review):** the pending multiline choice is decided — pasting `reddit.com↵abdes` is rejected, never merged. The website field no longer strips line breaks; the raw pasted value stays and the domain validator keeps Add disabled until the break is removed. Preserve-and-reject was chosen because stripping produced the forbidden merged domain and silently rejecting an update desyncs a value-based BasicTextField from state. Enter still inserts nothing; a domain with only surrounding whitespace still adds (canonical trim, UI-pinned) while the raw validator rejects untrimmed forms (JVM-pinned). Same checks: assembleDebug, 12 JVM executed green, androidTest compilation only. No device operations; APK not installed (no install requested — ready on owner word). Phase 3 base after approval: `6c3b962`.

**Round-two owner fixes — 13 September 2026, after the checkpoint above:** further owner device feedback landed as `4eea876` (still Phase 2 scope; Phase 3 assignment unaffected but the Phase 3 base moves to `4eea876`): website field single-line with paste-stripping plus complete-domain validation (`ui/util/Domain.kt`, 5-case JVM suite green — "reddit"/multiline rejected with an inline message); step-3 helper copy for Type to pause / Disable this block replaced (random-words wording); show-typos toggle removed, typos always shown (draft forces `show_typos = true`; column retained only to avoid a destructive wipe mid-testing — Phase 5 must ignore stored values, drop the column at the next schema revision); storage-side wait bound `MAX_STORED_COUNTDOWN_SECONDS = 1200` enforced on create/update (UI cap unchanged at 5:00, unmentioned on screen). Checks: assembleDebug, testDebugUnitTest (12 JVM green), assembleDebugAndroidTest compile; owner-requested reinstall performed. Reviewed with round three; see current checkpoint above.

**Round-three owner fixes — 13 September 2026, `084b5eb`:** friction estimate box now reads exactly "You'll take X seconds to type random words each time you want to pause this block." (X = 0.4 s/char estimate, plain seconds; sentence edit adopted from a parallel working-tree change); turn-on/off haptics via new `ui/util/Haptics.kt` (heavy click on, click off) at TurnOnScreen's enabling button and both turn-off switches; manifest gained the normal VIBRATE permission. Checks: assembleDebug, 12 JVM green, androidTest compile, hex clean. Reviewed; see current checkpoint above. GLM reports installation completed with data preserved.

## Next assignment — Phase 3 only

Use GLM 5.3 for permission state, activation gates and lifecycle handling. Phase 3 is cleared from approved `6c3b962`; verify git state. Build welcome, exactly four permission rows, accessibility explainer, manufacturer battery instructions/picker and Settings (PRD §§6/10/12, screens 1–4/24). Refresh system permission state on return; gate activation on accessibility, never app entry or creation. Preserve §17 refinements. State checks/stop boundaries before implementation. Device setup/testing is a separate owner-authorized handoff.

## Current responsibilities and boundary — latest owner update, 10 September 2026

Read AGENTS.md §§3a–3b: GLM implements, compiles and runs targeted non-device checks; Codex reviews code only; Arjun leads all device and experience checks after code clearance. Neither agent runs adb, installations, device/emulator operations or connected/instrumented tests without explicit owner authorization. These rules supersede historical hardware instructions and results below.

## Read in order

1. AGENTS.md in full: phase scope, build/device rules and acceptance gates.
2. PRD.md: behavioral authority, especially §4, Phase 2 screen descriptions and §10 events.
3. DECISIONS.md in full: prior decisions, owner decisions and known caveats.
4. Verification Feedback/phase-2-verification/REPORT.md: current actionable findings. Evidence files are supporting reproductions, not required reading unless diagnosing a finding.
5. The Nocturne readme/styles.css and relevant screen markup/renders before changing visuals.
6. Inspect git status, log and diff, then read the affected implementation/tests in full before editing.

## Important context

- Toolchain is already configured: Gradle 8.7, AGP 8.4.1, Kotlin 1.9.24, Compose compiler 1.5.14. Do not bump versions or add dependencies without owner approval.
- Original Phase 1 repairs: atomic failed saves, canonical domain identity, and target_type to exclude sites from the per-app leaderboard. Preserve those invariants. a0df32c restored meaningful failed-save rollback tests; preserve that coverage and address the latest targeted gaps.
- Phase 0 error palette has owner approval. Direct OFF is temporary until Phase 5; ON permission gating belongs to Phase 3. Stats/feedback belong to Phase 7.
- Current values: 30-character names; 0.4 seconds/character; pause minimum 5/default 15 minutes. PRD §17 supersedes historical mockup values.
- RESOLVED in a0df32c: the conflict flow (in-place Box) is removed; no modal paths remain in Phase 2 flows. DECISIONS.md now records the correction.
- Carry-forward (non-blocking, from the report): route and draft state use plain `remember` and die on activity recreation — saved-state preservation is wanted before release. Glyph-only clickable controls lack accessibility labels/roles — address as shared controls mature. Destructive migrations remain dev-only.

## Preserve unrelated work

At handoff, DECISIONS.md already has an owner branding update; old icon-concept files are deleted and design/icon-final is untracked. These are unrelated changes. Do not revert them or sweep them into a repair commit with git add -A.

The recorded owner decision is to rename the product to Time Shrine in a dedicated change; existing code/spec still say Toki Shrine and package com.arjunrana.tokishrine. The selected icon is the torii/hourglass reference under design/icon-final/master/. The selected raster icon is already integrated in 512dc36. Do not redo it or undertake the separate product rename during Phase 3.

## Keeping future sessions reliable

Treat repository documents and verified code as durable context. At every stop, record the current commit, uncommitted work, exact remaining task, tests actually run, decisions and unresolved issues. Keep this handoff short and current. Ask the outgoing builder to append any unrecorded discoveries before closing its session. A fresh session should first summarize its scope and findings back to Arjun, then carry out the authorized repair work. Do not claim access to the old conversation or assume every old completion claim was independently verified.

## Model and run-boundary reminder

Follow AGENTS.md §3c. At each phase/repair start, recommend the builder model with a reason and state scope, checks and stop condition. Codex remains the independent reviewer; switching builders does not trigger a whole-codebase review. Preserve the current repair and review its diff before requesting further implementation. After two failed attempts at the same check, report a bounded diagnosis instead of continuing speculative retries. Once planned checks pass, hand off. Phone preparation follows code approval and an owner setup request; it is not authorization to run device tests.

At phase/repair start, use the model/scope/checks/stop format in AGENTS.md §3c. Flash is the owner-reported image-reading choice for supplied visual references and bounded UI work; GLM 5.3 is preferred for complex state/lifecycle/persistence. Do not add a second whole-project review or require a model handoff for trivial work. On interruption, report from local records without launching another check or querying the phone.
