<?xml version="1.0" encoding="utf-8"?>
<!-- Phase 4 detection engine: window state/content events carry app
     switches and address-bar updates; flagReportViewIds makes
     findAccessibilityNodeInfosByViewId work for address-bar reading.
     The package list itself is applied at runtime via setServiceInfo —
     enabled blocked apps plus the supported browsers from the bundled
     JSON map, so the service stays idle for everything else (PRD §13). -->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true" />
